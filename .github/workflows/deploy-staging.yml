name: Deploy to Staging

on:
  workflow_dispatch:

jobs:
  deploy:
    name: deploy-staging
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Confirm gate
        run: echo "Staging deploy approved via GitHub Environment"